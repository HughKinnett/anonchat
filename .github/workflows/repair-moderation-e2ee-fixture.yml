name: Repair moderation E2EE fixture

on:
  push:
    branches:
      - fix/e2ee-spotify-privacy
    paths:
      - scripts/repair-moderation-e2ee-fixture.mjs
      - .github/workflows/repair-moderation-e2ee-fixture.yml

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/e2ee-spotify-privacy
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Repair moderation E2EE fixture
        run: node scripts/repair-moderation-e2ee-fixture.mjs
      - name: Validate patch
        run: |
          git diff --check
          git diff -- scripts/test-moderation-rules.mjs
      - name: Commit repaired fixture
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/test-moderation-rules.mjs
          git commit -m "test: use encrypted room messages in moderation rules"
          git push origin HEAD:fix/e2ee-spotify-privacy
