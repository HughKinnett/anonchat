name: Apply Phase B mutual follow suggestions

on:
  push:
    branches:
      - phase-b-content-discovery
    paths:
      - scripts/apply-phase-b-mutual-follows.mjs
      - .github/workflows/apply-phase-b-mutual-follows.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase-b-content-discovery
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: node scripts/apply-phase-b-mutual-follows.mjs
      - run: node scripts/test-phase-b-discovery-integration.mjs
      - run: node scripts/test-suggested-follow-policy.mjs
      - run: node --check timeline.js
      - name: Commit mutual-follow integration
        run: |
          if git diff --quiet; then
            echo "No mutual-follow changes required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add timeline.js
          git commit -m "feat: use real mutual-follow suggestion signals"
          git push origin HEAD:phase-b-content-discovery
