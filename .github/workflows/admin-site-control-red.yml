name: Admin site-control red check
on:
  push:
    branches:
      - feature/simplified-admin-command-center
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/test-admin-site-control-enforcement.mjs
